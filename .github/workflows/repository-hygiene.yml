name: Repository Hygiene

on:
  push:
    branches: [main]
  pull_request:

jobs:
  repository-hygiene:
    name: repository hygiene
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: ./scripts/repository-check.sh
